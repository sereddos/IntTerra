import Vue from 'vue'
import Vuex from 'vuex'
import operations from './operations'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    operations
  }
})
